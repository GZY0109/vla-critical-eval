# 工作约定

- **commit 不加 Co-Authored-By 或任何 AI 署名行**——只署我自己的 git 身份。
- `PROGRESS.md` 用第一人称写，当作自己的开发日志，不要出现"用户要求"、"assistant做了"这类第三方视角
  的措辞。
- 新会话开工前先读 `PROGRESS.md`"当前状态"和最近记录，跑 `git log --oneline -20` 核对代码进度和文档
  是否一致，对不上以 git log 为准并更正文档。
- 涉及具体数字/论文引用/模型能力这类可验证的断言，开工前先核实来源（读官方仓库/论文/官方文档），不要
  凭训练数据里的印象直接当结论用——这个项目定案过程中已经因为没核实而排除过一个不存在的复现目标
  （WholebodyVLA，仓库无代码），见 `PROGRESS.md`/`PROJECT_PLAN.md` 的记录。
